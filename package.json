{
  "name": "unibox-mvp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check server/database.js && node --check server/device-gateway.js && node --check src/app.js"
  },
  "engines": { "node": ">=24" }
}
