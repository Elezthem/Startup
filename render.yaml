services:
  - type: web
    name: unibox-mvp-startup
    runtime: node
    buildCommand: ""
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 24.16.0
